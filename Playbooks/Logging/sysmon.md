# Downloading Sysmon for Windows  
1. Download SysInternalSuite either from the web or an ```Invoke-WebRequest```  
2. Get or clone the config from SwiftOnSecurity
3. Run Sysmon with the config in the SysInternals directory ```./sysmon.exe -accetpeula -c "C:\path\to\config"```
