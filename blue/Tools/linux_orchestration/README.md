# Linux Orchestration
* `meow.sh` is the driver. Output logs and artifacts from the scripts are delivered to their corresponding directory
    * To run custom scripts, the driver expects them to be in the same directory as itself