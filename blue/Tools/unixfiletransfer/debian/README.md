# Debian scripts
 * `smb.sh` - Installs a Samba server. Creates a user called `backupuser`. 
    May insert a conflicting entry into `/etc/samba/smb.conf` if an SMB 
    server already exists.
 * `smb.sh` - Sets up a quick and dirty FTP server in `/tmp`
